package Model.Elements;

import Model.Position;

import java.awt.image.BufferedImage;

public class Key extends Element{
    public Key(Position position, BufferedImage image) {
        super(position, image);
    }
}
